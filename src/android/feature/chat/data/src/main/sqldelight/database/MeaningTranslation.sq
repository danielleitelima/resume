CREATE TABLE MeaningTranslation (
    id TEXT PRIMARY KEY NOT NULL,
    content TEXT NOT NULL,
    languagesCode TEXT NOT NULL
);

insert:
INSERT OR REPLACE
INTO MeaningTranslation
VALUES(?, ?, ?);

getById:
SELECT *
FROM MeaningTranslation
WHERE id = :id;