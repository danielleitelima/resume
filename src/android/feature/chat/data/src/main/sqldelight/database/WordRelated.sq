import kotlin.Boolean;

CREATE TABLE WordRelated (
    id TEXT PRIMARY KEY NOT NULL,
    content TEXT DEFAULT NULL,
    strong INTEGER AS Boolean DEFAULT 0,
    type TEXT DEFAULT NULL
);

insert:
INSERT OR REPLACE
INTO WordRelated
VALUES(?, ?, ?, ?);

getById:
SELECT *
FROM WordRelated
WHERE id = :id;
