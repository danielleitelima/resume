CREATE TABLE WordSubDefinitionTranslation (
    id TEXT PRIMARY KEY NOT NULL,
    content TEXT DEFAULT NULL,
    languagesCode TEXT NOT NULL
);

insert:
INSERT OR REPLACE
INTO WordSubDefinitionTranslation
VALUES(?, ?, ?);

getById:
SELECT *
FROM WordSubDefinitionTranslation
WHERE id = :id;
