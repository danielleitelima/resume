CREATE TABLE ExpressionTranslation (
    id TEXT PRIMARY KEY NOT NULL,
    description TEXT NOT NULL,
    languagesCode TEXT NOT NULL
);

insert:
INSERT OR REPLACE
INTO ExpressionTranslation
VALUES(?, ?, ?);

getById:
SELECT *
FROM ExpressionTranslation
WHERE id = :id;